# Core dependencies
numpy>=1.24
opencv-python>=4.8
pyyaml>=6.0
pydantic>=2.0
pydantic-settings>=2.0

# Deep learning - using ONNX for cross-platform GPU support
onnxruntime-directml>=1.16; sys_platform == "win32"
onnxruntime-gpu>=1.16; sys_platform == "linux"
torch>=2.0
torchvision>=0.15

# Pose estimation
# mmpose and mmcv for RTMPose (will be installed separately)

# OSC for VRChat communication
python-osc>=1.8

# 3D Visualization for demo
pyvista>=0.42
open3d>=0.17

# Temporal filtering
scipy>=1.11

# GUI
dearpygui>=1.10

# Development
pytest>=7.0
